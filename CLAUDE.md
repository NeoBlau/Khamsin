# Хамсин — заметки для работы над кодом

Курьерский симулятор по пустыне на Godot 4.5.1. Основная платформа — macOS
(Metal). Общее описание в README.md, здесь только то, что нужно знать, прежде
чем что-то менять.

## Порядок работы

```bash
tools/test.sh                  # обязательно перед каждым коммитом
tools/test.sh --filter tire    # часть тестов
```

`tools/test.sh` сам делает `--import`. **Это не формальность**: после
добавления нового `class_name` Godot не видит его до импорта, и половина
проекта «не компилируется» без единой настоящей ошибки.

Скрипты проверяются только внутри поднятого дерева сцены. `godot --check-only`
здесь бесполезен: он не знает про автолоады, и каждое обращение к `Config` или
`EventBus` выглядит для него ошибкой.

## Что сломается тихо

Каждый пункт ниже — реальный баг из истории проекта. Все закрыты тестами, но
их легко вернуть.

- **Свежий клон без импорта.** Без каталога `.godot` нет кеша глобальных
  классов: типы, объявленные через `class_name`, движку неизвестны, автолоады
  не компилируются, и игра уходит в поток `Invalid access to property or key
  on a base object of type 'Nil'` — по ошибке на кадр. Повторный запуск не
  помогает: плоский `godot --path .` кеш не создаёт никогда. Запускать через
  `tools/run.sh`, он делает импорт сам; в CI это проверяется прогоном на
  чистом чекауте.
- **Тест с `await` — это корутина**, и Godot возвращает для неё не `Signal`, а
  объект `GDScriptFunctionState` с сигналом `completed`. Раннер обязан ждать
  оба вида; иначе асинхронные тесты не выполняются и всё равно считаются
  пройденными.
- **`set_anchors_preset` не растягивает контрол.** Он переносит якоря и
  сбрасывает отступы в минимальный размер, то есть в ноль. Рисование при этом
  честно вызывается и честно ничего не рисует. Растягивает только
  `set_anchors_and_offsets_preset`.
- **Поворот вокруг Y на угол α** отображает местную ось +Z в `(sin α, 0, cos α)`.
  Если направление в плане задано как `(cos α, 0, sin α)`, нужен поворот на
  `π/2 − α`. Перепутать легко, в коде незаметно, в кадре — сразу: ряд объектов
  складывается в зигзаг.
- **Развал, увод и всё зеркальное по бортам** обязано менять знак вместе с
  бортом. Тяга от развала без зеркала толкает все четыре колеса в одну
  сторону, и машину на прямой сносит вбок.
- **Габаритная коробка кузова** у фургона начинается на уровне пола салона, а
  не у земли. Если задать её низ ниже осей, колёса окажутся внутри кузова, и
  снаружи их просто не будет видно.
- **Очередь задач `WorkerThreadPool` переживает узел.** Если освободить узел,
  пока задача считает, она обратится к исчезнувшему мьютексу. Дожидаться в
  `_exit_tree`.
- **Открытый экран ставит дерево на паузу.** В тестах это значит, что у
  следующего теста машина просто не падает, и симптом выглядит как
  «подвеска не работает».
- **Обход треугольников.** Лицевая грань в Godot — по часовой стрелке.
  Обратный порядок не даёт ни ошибки, ни предупреждения: поверхность просто
  перестаёт рисоваться, и в кадре остаются торчать юбки чанков, которые легко
  принять за рельеф. Соглашение проверяется по эталонному `PlaneMesh`.
- **`return` в fragment-шейдере** запрещён. Шейдер с ним не собирается, и
  материал молча перестаёт рисоваться.
- **Точность шума в шейдерах.** Карта двадцать километров: `fract(p * 123.45)`
  на координатах в тысячи метров вырождается в константу. Решётку заворачивать
  в период, множители держать меньше единицы.
- **Высотный туман** в Godot сгущается *ниже* `fog_height`, а не выше.
- **Демпфирование скорости** у `RigidBody3D` включено по умолчанию (0.1).
  Для машины это несколько килоньютонов встречной силы: сопротивление здесь
  считается явно, поэтому демпфирование выключено насовсем.
- **Приведённая инерция.** Момент через передачу 25:1 приходит на колесо
  умноженным в двадцать пять раз. Без инерции двигателя, приведённой к колесу,
  петля обратной связи идёт вразнос и колёса начинают крутиться назад.
- **Решения по оборотам двигателя.** При разомкнутом сцеплении и при буксующем
  колесе двигатель свободно раскручивается до отсечки. Всё, что зависит от
  «сколько сейчас оборотов», надо считать от скорости колёс.
- **Лямбда захватывает локальные переменные по значению.** Присваивание
  внутри лямбды снаружи не видно, и тест, собирающий результат в булеву
  переменную, всегда показывает «ничего не произошло». Массив или словарь
  захватываются по ссылке — складывать результат надо в них.
- **Многострочные лямбды внутри литералов массивов** ломают разбор GDScript.
  Выносите лямбду в переменную.
- **Целые константы** больше `int64.max` (`0x9E3779B97F4A7C15`) GDScript не
  принимает — писать знаковым десятичным.
- **`b | 1`** как способ сделать множитель нечётным склеивает 0 с 1 и 2 с 3.
  Для перемешивания брать `b * 2 + 1`.
- **Деформация грунта — петля с обратной связью.** Если записывать в колею всю
  просадку колеса, получается усиление: колея глубже -> опора ниже -> колесо
  проседает сильнее. Машина уходит в песок по мосты, стоя на месте, а тест на
  переворот падает на первом же прогоне. В грунте остаётся только
  пластическая доля (`SandField.PLASTIC_FRACTION`), и рыхлость не должна
  подмешиваться сама в себя.
- **Стоящее колесо не роет колею.** Его просадка уже посчитана в модели шины
  и уже учтена в радиусе качения; записать её ещё и в грунт — посчитать одно
  и то же дважды. Машина, простоявшая минуту на рыхлом песке, после этого не
  трогается с места вообще. Грунт перемещает только работающее колесо:
  катящееся по новому месту или срезающее его буксованием.
- **Бруствер — вбок и назад, никогда вперёд**, и высота его берётся
  максимумом, а не суммой. Валик перед колесом означает, что машина едет по
  собственной насыпи; сумма вместо максимума упирает его в потолок за долю
  секунды, потому что физику опрашивают сто двадцать раз в секунду. Оба раза
  симптом один: спущенные колёса «перестают помогать».
- **Удар не равен нагрузке.** Пластическая деформация идёт за длительной
  нагрузкой, а не за пиком: без фильтра по времени приземление с четырёх
  метров штампует яму предельной глубины, два колеса повисают в воздухе, и
  дальше машина никуда не едет.
- **Дублированные вершины у примитивов Godot.** У `SphereMesh` шов по долготе
  и оба полюса — это несколько вершин в одной точке, различаются только
  развёрткой. Смещать их по номеру вершины нельзя: они разъезжаются, и в меше
  открывается щель с ровными краями, сквозь которую видно фон. Смещение
  считать от координат. Ни проверка намотки, ни проверка нормалей этого не
  ловят: с оставшейся геометрией всё в порядке, её просто стало меньше.
- **`SandField` переживает тест.** Следы в песке глобальные и живут, пока их
  не стёрли. `before_each`, который их не чистит, начинает случай в чужой
  колее, и тесты начинают зависеть от порядка.
- **Плечо момента — не расчётная длина пятна.** Модель пятна на спущенном
  колесе под нагрузкой выдаёт до трёх четвертей метра; принять это за плечо
  стабилизирующего момента — получить его в разы больше настоящего.
  Пневматический след — шестая часть полной длины пятна и не больше
  одиннадцати сантиметров.
- **`Input.action_press()` не порождает событие.** Состояние действия
  обновляется, но `event.is_action_pressed()` в `_unhandled_input` не срабатывает,
  потому что события нет. Синтезировать надо `InputEventAction` через
  `Input.parse_input_event()` — и не изнутри `_input`, а опросом в `_process`,
  иначе получается повторный вход в обработку ввода.
- **Аккорд в `InputMap` не выражается.** Если у кнопки есть базовая привязка в
  карте ввода, то модификатор её не отменит: состояние действия обновляется
  раньше, чем узел успевает вмешаться, и BACK+A дёргает ручник заодно с
  блокировкой. Поэтому кнопок геймпада в `project.godot` нет вовсе — раскладка
  целиком в `src/core/pad_layer.gd`.

## Правила проекта

- Ни одного магического числа в коде машины и мира: всё в `data/*.json` и в
  `src/core/config.gd`.
- Ни одной случайности мимо `Rng`. Мир, доска заказов и погода выводятся из
  сида, поэтому сейв хранит число, а не состояние.
- Рельеф — чистая функция координат. Её зовут генератор меша, генератор
  коллизии, маршруты и запрос «что под колесом»; расхождению взяться неоткуда,
  и так должно остаться.
- Интерфейс собирается кодом (`src/ui/widgets.gd`), сцены экранов — пустые
  обёртки со скриптом.
- Условия и последствия в сюжете — фиксированный набор ключей, а не язык
  внутри JSON. Неизвестный ключ должен ругаться, а не молчать.

## Инструменты

```bash
# запуск (импортирует ресурсы, если каталога .godot ещё нет)
tools/run.sh

# стенд телеметрии: разгон, тормозной путь, давление в шинах, предельный уклон
godot --headless --path . res://tools/bench_vehicle.tscn

# снимок мира без запуска игры руками
xvfb-run -a godot --path . --rendering-driver vulkan \
  --resolution 1280x720 res://tools/screenshot.tscn -- \
  --out /tmp/shot.png --hour 8.5 --drive 6 --screen contracts
```

Стенд — не тест: он измеряет, а не утверждает. Пороговые проверки живут в
`tests/`. При этом именно он нашёл четыре бага трансмиссии, которых не видно
ни в коде, ни в тестах: числа врут заметнее, чем логика.
